NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# Google OAuth config (chỉ cần Client ID — luồng ID token không dùng client secret ở phía browser)
NEXT_PUBLIC_GOOGLE_OAUTH_CLIENT_ID=

# R2 public URL (để dùng cho các file tĩnh, ví dụ ảnh, video, v.v.)
NEXT_PUBLIC_R2_PUBLIC_URL=
